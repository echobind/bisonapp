{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@prisma",
      "**/@prisma/**"
    ]
  },
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "publish:canary": "yarn lerna publish --conventional-prerelease --preid canary --pre-dist-tag canary",
    "publish": "yarn lerna publish --conventional-graduate",
    "test": "yarn lerna run test --stream"
  },
  "devDependencies": {
    "conventional-changelog-conventionalcommits": "^4.4.0",
    "lerna": "^3.22.1"
  }
}
