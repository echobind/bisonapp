---
to: tests/factories/<%= name %>.ts
---
<% single = h.inflection.singularize(name) -%>
<% camelized = h.inflection.camelize(name) -%>
// import Chance from 'chance';

import { create<%= camelized %>Factory } from '@/prisma/factories';

// const chance = new Chance();

export const <%= camelized %>Factory = create<%= camelized %>Factory({});
